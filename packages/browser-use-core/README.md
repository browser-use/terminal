# browser-use-core

Packaged Browser Use Rust terminal binaries and Python worker resources.

This package is a binary/resource carrier for `browser-use`. It intentionally does
not expose the high-level Python agent API; that API stays in `browser-use`.
